import {WatchlistComponent} from './watchlist-component';

describe('WatchlistComponent', () => {
  it('should create an instance', () => {
    expect(new WatchlistComponent()).toBeTruthy();
  });
});
